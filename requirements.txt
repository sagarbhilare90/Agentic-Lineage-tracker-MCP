# Core
sqlglot>=23.0.0
networkx>=3.2
python-dotenv>=1.0.0

# LLM (optional – only needed if ANTHROPIC_API_KEY or OPENAI_API_KEY is set)
anthropic>=0.25.0
langchain>=0.2.0
langchain-anthropic>=0.1.0
langchain-openai>=0.1.0
langgraph>=0.1.0

# Snowflake (optional – only needed when DEMO_MODE=false)
# snowflake-connector-python>=3.6.0

# Neo4j (optional – only needed when GRAPH_BACKEND=neo4j)
# neo4j>=5.18.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
